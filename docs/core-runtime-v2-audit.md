# Core Runtime v2 completion audit

This is the evidence ledger for `INTENT.md` and `IMPLEMENTATION_PLAN.md`. A
green current test suite is necessary but does not mark an incomplete
workstream complete. Every row becomes complete only when its implementation,
negative cases, acceptance application, and required platform gate exist.

Last updated: 2026-09-05.

| Workstream | Status | Evidence present | Blocking gaps |
| --- | --- | --- | --- |
| A. Rhai boundary | Partial | Rhai is pinned to 1.26.0; volatile native call context is isolated in `invocation.rs`; retained callback closure-rejection/curry/import and effect start-failure/restart/hot-reload/dispose async-ownership matrices exist; Engine `on_progress` records actual per compile/render/lifecycle/callback operation totals in ExecutionTiming and Inspector, including imported nested work; all production named entry/lifecycle/root calls cross a static AST-oracle `ExecutionBackend`; opt-in `grain-backend` parity covers self-contained imports, native dispatch, named calls, FnPtr curry, structured values and errors while recording residual counts | Grain currently changes characterized error diagnostics and therefore remains disabled; imported component/effect/callback, progress-limit, reload-failure, source-position, artifact, and end-to-end performance parity are not certified |
| B. Retained tree | Complete | monotonic `NodeId`; atomic keyed reconciliation; group-local key identity; duplicate/type/reorder rollback tests; lifecycle/renderer consume `RetainedUiTree`; retained nodes own event/ref/capture/scroll/primitive metadata plus semantic attributes/static text; lifecycle primitive identity includes stable `NodeId`, preserves same-local-key instances in separate branches and keyed reorder, and event emitters weak-reference their registry to break Entity cycles; Host budgets independently cap Layer count, Canvas scene count, and Canvas command/segment complexity; deterministic last-success report and bounded successful-commit Inspector mutation traces | — |
| C. Components/dependencies/effects | Complete | `define_component`/`render_component`; pre-evaluation imported-module purity rejects mutable globals, control flow, arbitrary calls, computed definitions, and duplicate definitions while accepting literal constants/imports/exports/direct declarations; closed structural prop union; retained invocation recipes; topmost dirty rerender plus root-dirty props/environment/dirty-descendant bailout before Rhai component execution, with transparent child roots promoted to the nearest replaceable ancestor and conservative slot comparison plus complete state/dependency/callback/effect/timer/signal/ref/virtual ownership reuse; whole-field and bounded nested Map/Array store dependencies, including reorder-stable keyed-item selectors, stale-reader cleanup, equal-write suppression, schema-checked path replacement, and transactional rollback; exact locale app/window/subtree, discrete viewport-class, and retained-geometry dependencies; native-only locale direction repaint is separate from Rhai dirty work, while symbolic theme tokens resolve in the renderer; declarative named effects with dependency restart, imported-context cleanup, failure rollback, 64-transition budget, distinct activation scopes that automatically own tasks/subscriptions/image decodes, and Inspector dependency/callback/activation identity; keyed one-shot timers preserve deadlines, pause/completion, generation, rollback, scope cleanup, and Inspector state; deterministic differential and nested-bailout tests prove exact component rerenders and reuse preserve full `UiNode`, state, invocation identities, dependencies, and owned work | — |
| D. Atomic visual surface | Partial | final `Box`/Fragment and helpers; typed Style maps bounded flex/grid/layout with property-specific non-negative `Length`, size/flex/margin/inset-only `AutoLength`, and margin/inset-only bounded `SignedLength`, all retained losslessly as `LayoutLength`; two-stop gradient/shadow/opacity/cursor/typography/clamp/translation, physical/logical per-edge border widths, uniform solid/dashed border style, per-corner radii, GPUI block/block-except-scroll occlusion, plus validated font family/fallback stack/weight/slant/OpenType features; GPUI 0.2.2 was source-audited to expose only one quad-wide border color/style; strict shared hex/name/rgb(a)/hsl(a)/hwb ColorValue literals; validated file/embedded font loading and CLI artifacts; bounded self-contained inline `svg` atom parses with GPUI's `usvg`, rejects active/external content, and resolves `currentColor`; plain Text plus inline Span runs; retained keyed Canvas rect/circle/line and arbitrary fill/stroke quadratic/cubic/close paths with solid/two-stop gradient, uniform scale/rotate/translate, axis-aligned clip, segment-inclusive budgets, analytic/flattened reverse-order hit testing, and pointer `canvas_key`; namespaced theme tokens; artistic showcase consumes gradient/shadow/path/hit/typography in pure Rhai | GPUI-limited intrinsic/min-content/max-content and custom fr/grid-track values, multi-stop gradients, normal-node scale/rotation/origin/z-index, and per-edge border colors/styles; Lab/LCH/OKLab color policy; font aliases and dev hot replacement; text links/selection/search; Canvas per-command semantics/a11y/signals, stroke detail, nested groups |
| E. Signals/animation | Partial | component/key/type-scoped `NativeSignal`; Rhai and Rust foreground writes; rollback and stale-handle behavior; approved width/height/opacity/translation/color bindings sampled without Rhai rerender; compatible identity retention tests; Inspector reports signal value/type/revision/last Script-or-Host writer; timers and animation share a Host-injected monotonic `RuntimeClock`; Canvas scene rotation drives Spinner without Rhai frame callbacks and reduced motion settles visibly; Inspector reports active/settled animation state | generalized property-source ownership; transitions/springs/keyframes/derived sources on final atoms; general non-Canvas transforms; scroll/Canvas signal bindings; enter/exit/layout/shared-layout behavior |
| F. Events/refs/focus/scroll/a11y | Partial | ordered capture/target/bubble and independent responses; normalized pointer/window/local plus scroll-adjusted content coordinates; raw pointer/wheel payloads, Script callbacks, NativeEvent, and automation expose event-time current-target bounds; Canvas hit/capture; retained refs/geometry/focus/scroll/reveal and Layer; per-axis themed overlay scrollbars with auto/always/hidden visibility, track paging, thumb dragging and RTL edge placement on the same ScrollHandle; event-coordinate Overlay anchors, logical edge Sheet layout, bounded tab order and modal trap/restore; pressed/expanded/orientation/range metadata in the flattened AccessibilityTree | future general transforms plus non-mouse/multi-pointer input; pointer cancel/coalescing/frame accounting; reusable nonmodal focus scopes and generalized grid focus; nested scroll containment/signals; GPUI/AccessKit forwarding and Canvas/virtual semantic projection |
| G. Native mechanisms | Partial | keyed custom primitive lifecycle uses retained `NodeId` identity, normalized snapshots, panic boundaries, weak event-emitter ownership and resource scopes; native RangeInput retains hot drag preview and emits one controlled commit; variable-height `virtual_collection` preserves GPUI measurements, bounded realization, sticky headers and retained item identity; NativeCollection caches Table sort/group/collapse and Command fuzzy/group/navigation orders separately from visible selection/active projection; deterministic Fenwick anchor/tail core with 10k tests; shared native Input/Textarea core covers grapheme/UTF-16/IME/clipboard/limits plus bounded undo/redo, typing coalescing, composition and controlled reset | complete no-empty-frame/follow-tail benchmark; shared document-text infrastructure |
| H. Registry migration | Complete | all 46 official components are inspectable Rhai source over public atoms/mechanisms; Dropdown was destructively renamed to strictly controlled Combobox; Alert/status/surface/group/input/toggle/range/scroll/command/context-menu/sheet families have CLI, metadata and Theme Studio integration; old eager VirtualList and specialized high-level native component constructors remain deleted; an API-surface test rejects their reintroduction | — |
| I. Tooling/apps | Partial | component/manifest/theme/locale/asset/font checks; `check` validates known call arities and executes real initial lifecycle/reconcile; Metadata emits schema/snippet JSON plus Rhai 1.26 definitions; copied source remains indexable; Inspector and automation expose retained identity/state/geometry/mechanisms; pure-Rhai showcase/Timeline/Chat apps; Component Gallery reuses Theme Studio's 46-component specimen with category navigation and 15 live themes | GPU screenshot/readback and raw platform input commands; 120 Hz interaction certification; Rust fast-path Timeline parity app |
| J. Certification | Partial | all-feature workspace/all-target suite passes with 275 core unit tests, 40 official-registry cases, every integration target, and all 19 product examples; strict all-target/all-feature Clippy and `RUSTDOCFLAGS=-Dwarnings` docs pass; the independent workspace passes 40 real GPUI interaction cases plus two example-preparation guards, including Slider LTR/RTL, ScrollArea, ContextMenu, Sheet, CommandDialog, and live Component Gallery category/theme switching; the core crate package verifies; release example/CLI builds, all 19 example event-loop smokes, Theme Studio, DataTable state smokes, and 11 embedded artifact leakage audits pass; all 38 existing macOS PNG baselines pass dimension/count audit; the unlocked 2026-09-05 manual pass covered Theme Studio plus every Gallery category, all 15 themes, compact/regular resize, Arabic RTL, reduced motion, Sheet, CommandDialog, and AlertDialog | checked-in Studio/Gallery baseline expansion; final API matrices and 120 Hz certification |

## Current automated baseline

Run from the repository root:

```sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --features dev-reload -- -D warnings
cargo test --workspace --all-targets
RUSTDOCFLAGS=-Dwarnings cargo doc --workspace --no-deps
cargo build --workspace --all-targets --release
cargo test -p gpui-rhai --features grain-backend backend::tests
cargo clippy -p gpui-rhai --all-targets --features "dev-reload grain-backend" -- -D warnings
cargo test --manifest-path tests/native-keyboard/Cargo.toml
cargo clippy --manifest-path tests/native-keyboard/Cargo.toml --tests -- -D warnings
bash scripts/audit-release-artifacts.sh
bash scripts/audit-visual-baselines.sh
bash scripts/release-smoke.sh
```

The audit is complete only when every row is complete and the acceptance gates
in section 13 of `IMPLEMENTATION_PLAN.md` pass without exclusions.
