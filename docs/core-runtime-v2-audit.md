# Core Runtime v2 completion audit

This is the evidence ledger for `INTENT.md` and `IMPLEMENTATION_PLAN.md`. A
green current test suite is necessary but does not mark an incomplete
workstream complete. Every row becomes complete only when its implementation,
negative cases, acceptance application, and required platform gate exist.

Last updated: 2026-08-31.

| Workstream | Status | Evidence present | Blocking gaps |
| --- | --- | --- | --- |
| A. Rhai boundary | Partial | Rhai is pinned to 1.26.0; volatile native call context is isolated in `invocation.rs`; retained callback closure/curry/import tests exist; Engine `on_progress` records actual per compile/render/lifecycle/callback operation totals in ExecutionTiming and Inspector, including imported nested work | stale effect/reload matrix; backend trait and optional Grain parity harness |
| B. Retained tree | Partial | monotonic `NodeId`; atomic keyed reconciliation; group-local key identity; duplicate/type/reorder rollback tests; lifecycle/renderer consume `RetainedUiTree`; retained nodes own event/ref/capture/scroll metadata plus semantic attributes/static text used by stable AccessibilityTree; Host-configurable budgets for nodes/handlers/components/effects/timers/signals/refs | retained primitive Entity handles, separate Layer/Canvas budget classes, diff metrics, and Entity cycle gate remain |
| C. Components/dependencies/effects | Partial | `define_component`/`render_component`; closed structural prop union; retained invocation recipes; topmost dirty rerender; field/store invalidation; declarative named effects with dependency restart, imported-context cleanup, failure rollback, and 64-transition budget; keyed declarative one-shot timers preserve deadlines, reconcile signature/pause state, retain completion, deliver named callbacks on foreground, and roll back/clean with component scope | theme/locale/viewport/geometry dependency graph; broad Map/Array path APIs; effect-owned async scopes and full Inspector data; full-render equivalence/property tests; module-init purity audit |
| D. Atomic visual surface | Partial | final `Box`/Fragment and helpers; typed Style maps bounded flex/grid/layout, gradient/shadow/opacity/cursor/typography/clamp/translation; strict shared hex/name/rgb(a)/hsl(a) ColorValue literals; validated file/embedded font loading and CLI artifacts; plain Text plus inline Span runs; retained keyed Canvas rect/circle/line and arbitrary fill/stroke quadratic/cubic paths with solid/two-stop gradient, uniform scale/rotate/translate, axis-aligned clip, segment-inclusive budgets, analytic/flattened reverse-order hit testing, and pointer `canvas_key`; namespaced theme tokens; artistic showcase consumes gradient/shadow/path/hit/typography in pure Rhai | property-specific signed/intrinsic/auto/fr/grid-track values; multi-stop gradients, scale/rotation for normal nodes, origin, detailed borders, hit/stacking controls; advanced color spaces; font aliases/fallback/features and dev hot replacement; text links/selection/search; svg atom; Canvas per-command semantics/a11y/signals, stroke detail, nested groups |
| E. Signals/animation | Partial | component/key/type-scoped `NativeSignal`; Rhai and Rust foreground writes; rollback and stale-handle behavior; approved width/height/opacity/translation/color bindings sampled without Rhai rerender; compatible identity retention tests; legacy node animation runtime and deterministic unit tests | generalized property-source ownership; transitions/springs/keyframes/derived sources on final atoms; scroll/Canvas bindings; writer/clock Inspector data; enter/exit/layout/shared-layout behavior |
| F. Events/refs/focus/scroll/a11y | Partial | ordered capture/target/bubble binding model; independent response controls; real GPUI pointer normalization; committed layout/translated visual bounds drive local coordinates; capture preserves Canvas hit payload; low-frequency hover; Host/NativeHandlerRef transaction; retained refs, geometry, focus/scroll/reveal commands and generic Layer; stable layout-flattened AccessibilityTree resolves relationships/geometry, rejects duplicate semantic IDs, and exposes role/name/ID queries | nested-scroll content coordinates and future rotation/scale plus non-mouse/multi-pointer input; pointer cancel/coalescing/frame accounting; public focus scopes/traps/restore/roving/directional behavior; nested scroll boundary/containment/signals; GPUI/AccessKit forwarding and Canvas/virtual semantic projection |
| G. Native mechanisms | Partial | keyed custom primitive mount/update(previous,next)/render/unmount with retained normalized snapshots and panic boundaries; data-backed variable-height `virtual_collection` realizes requested foreground windows and preserves GPUI measurements; deterministic Fenwick anchor/tail core with 10k tests; shared native Input/Textarea core covers grapheme/UTF-16/IME/clipboard/limits plus bounded undo/redo, typing coalescing, one-revision composition and controlled reset | explicit task/subscription/capture/resource ownership on primitive instances and Entity-cycle gate; expose virtual measurement/window metrics and complete no-empty-frame/follow-tail benchmark; shared document-text infrastructure |
| H. Registry migration | Complete | every registry component is inspectable Rhai source over public atoms/mechanisms; old eager VirtualList and all specialized Table/choice/date/toast constructors, UiNodeKind variants, renderers, Entities/queues/public Rust models, private assets, and private callback adapters are deleted; an API-surface test rejects reintroduction of privileged constructor names; CLI and copied-source examples verify public transitive dependencies | — |
| I. Tooling/apps | Partial | component/manifest/theme/locale/asset/font checks; `check` executes real initial lifecycle/reconcile; Metadata emits schema/snippet JSON plus official Rhai 1.26 `.d.rhai` definitions for every registered core API, and configured Hosts can emit extension-inclusive definitions; copied component source remains directly indexable; Inspector baseline and retained accessibility queries; pure-Rhai showcase/Timeline/Chat acceptance apps | screenshot and 120 Hz interaction certification; known-call/arity lint wired to the same definitions; automation input/actions/screenshots and JSON protocol; Rust fast-path Timeline parity app |
| J. Certification | Partial | debug and release workspace/all-target tests pass; strict all-target Clippy with dev-reload passes; `RUSTDOCFLAGS=-Dwarnings` workspace docs and release build pass; unattended release-smoke launches every product example and data-table state without panic; current release policy/reconciler probe is recorded | final API/node/style/event matrices and property/failure expansions; deterministic screenshot/accessibility/hot-reload resource evidence; pure-Rhai Timeline GPUI flush/layout/paint p95 and 120 Hz result; unlocked macOS theme/locale/RTL/resize/reduced-motion/IME/manual interaction gate |

## Current automated baseline

Run from the repository root:

```sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --features dev-reload -- -D warnings
cargo test --workspace --all-targets
```

The audit is complete only when every row is complete and the acceptance gates
in section 13 of `IMPLEMENTATION_PLAN.md` pass without exclusions.
