# Core Runtime v2 completion audit

This is the evidence ledger for `INTENT.md` and `IMPLEMENTATION_PLAN.md`. A
green current test suite is necessary but does not mark an incomplete
workstream complete. Every row becomes complete only when its implementation,
negative cases, acceptance application, and required platform gate exist.

Last updated: 2026-08-31.

| Workstream | Status | Evidence present | Blocking gaps |
| --- | --- | --- | --- |
| A. Rhai boundary | Partial | Rhai is pinned to 1.26.0; volatile native call context is isolated in `invocation.rs`; retained callback closure/curry/import tests exist; Engine `on_progress` records actual per compile/render/lifecycle/callback operation totals in ExecutionTiming and Inspector, including imported nested work | stale effect/reload matrix; backend trait and optional Grain parity harness |
| B. Retained tree | Complete | monotonic `NodeId`; atomic keyed reconciliation; group-local key identity; duplicate/type/reorder rollback tests; lifecycle/renderer consume `RetainedUiTree`; retained nodes own event/ref/capture/scroll/primitive metadata plus semantic attributes/static text; lifecycle primitive identity includes stable `NodeId`, preserves same-local-key instances in separate branches and keyed reorder, and event emitters weak-reference their registry to break Entity cycles; Host budgets independently cap Layer count, Canvas scene count, and Canvas command/segment complexity; deterministic last-success report and bounded successful-commit Inspector mutation traces | — |
| C. Components/dependencies/effects | Partial | `define_component`/`render_component`; closed structural prop union; retained invocation recipes; topmost dirty rerender; exact field/store, locale app/window/subtree, discrete viewport-class, and retained-geometry dependencies with stale-reader cleanup and transactional rollback; native-only locale direction repaint is separate from Rhai dirty work, while symbolic theme tokens resolve in the renderer; declarative named effects with dependency restart, imported-context cleanup, failure rollback, 64-transition budget, and Inspector dependency/callback identity; keyed one-shot timers preserve deadlines, pause/completion, generation, rollback, scope cleanup, and Inspector state | broad Map/Array path APIs and keyed-collection accessors; effect-owned async scopes; full-render equivalence/property tests; module-init purity audit |
| D. Atomic visual surface | Partial | final `Box`/Fragment and helpers; typed Style maps bounded flex/grid/layout, gradient/shadow/opacity/cursor/typography/clamp/translation plus validated font family/fallback stack/weight/slant/OpenType features; strict shared hex/name/rgb(a)/hsl(a) ColorValue literals; validated file/embedded font loading and CLI artifacts; bounded self-contained inline `svg` atom parses with GPUI's `usvg`, rejects active/external content, and resolves `currentColor`; plain Text plus inline Span runs; retained keyed Canvas rect/circle/line and arbitrary fill/stroke quadratic/cubic paths with solid/two-stop gradient, uniform scale/rotate/translate, axis-aligned clip, segment-inclusive budgets, analytic/flattened reverse-order hit testing, and pointer `canvas_key`; namespaced theme tokens; artistic showcase consumes gradient/shadow/path/hit/typography in pure Rhai | property-specific signed/intrinsic/auto/fr/grid-track values; multi-stop gradients, scale/rotation for normal nodes, origin, detailed borders, hit/stacking controls; advanced color spaces; font aliases and dev hot replacement; text links/selection/search; Canvas per-command semantics/a11y/signals, stroke detail, nested groups |
| E. Signals/animation | Partial | component/key/type-scoped `NativeSignal`; Rhai and Rust foreground writes; rollback and stale-handle behavior; approved width/height/opacity/translation/color bindings sampled without Rhai rerender; compatible identity retention tests; Inspector reports signal value/type/revision/last Script-or-Host writer; timers and animation share a Host-injected monotonic `RuntimeClock` with a deterministic manual implementation; legacy node animation runtime and deterministic unit tests | generalized property-source ownership; transitions/springs/keyframes/derived sources on final atoms; scroll/Canvas bindings; clock/animation Inspector detail; enter/exit/layout/shared-layout behavior |
| F. Events/refs/focus/scroll/a11y | Partial | ordered capture/target/bubble and independent responses; normalized pointer/local/Canvas hit/capture; hover and Host/native transactions; retained refs/geometry/focus/scroll/reveal and Layer; explicit bounded `tab_index`, native `tab_group`, tab-stop policy, modal Overlay trap/restore; stable flattened AccessibilityTree relationships/geometry and role/name/ID queries | nested-scroll content coordinates and future rotation/scale plus non-mouse/multi-pointer input; pointer cancel/coalescing/frame accounting; reusable nonmodal focus scopes and generalized roving/directional/grid focus; nested scroll boundary/containment/signals; GPUI/AccessKit forwarding and Canvas/virtual semantic projection |
| G. Native mechanisms | Partial | keyed custom primitive mount/update(previous,next)/render/unmount uses retained `NodeId` identity with normalized snapshots, panic boundaries, separate same-local-key instances, and weak event-emitter ownership; data-backed variable-height `virtual_collection` realizes requested foreground windows, preserves GPUI measurements, and carries retained item/subtree IDs into slot rendering; deterministic Fenwick anchor/tail core with 10k tests; shared native Input/Textarea core covers grapheme/UTF-16/IME/clipboard/limits plus bounded undo/redo, typing coalescing, one-revision composition and controlled reset | explicit task/subscription/capture/resource ownership on primitive instances; expose virtual measurement/window metrics and complete no-empty-frame/follow-tail benchmark; shared document-text infrastructure |
| H. Registry migration | Complete | every registry component is inspectable Rhai source over public atoms/mechanisms; old eager VirtualList and all specialized Table/choice/date/toast constructors, UiNodeKind variants, renderers, Entities/queues/public Rust models, private assets, and private callback adapters are deleted; an API-surface test rejects reintroduction of privileged constructor names; CLI and copied-source examples verify public transitive dependencies | — |
| I. Tooling/apps | Partial | component/manifest/theme/locale/asset/font checks; `check` walks unoptimized Rhai ASTs and validates known direct/method/imported call arities from the same Engine metadata as definitions, then executes real initial lifecycle/reconcile; Metadata emits schema/snippet JSON plus official Rhai 1.26 `.d.rhai` definitions for every registered core API, and configured Hosts can emit extension-inclusive definitions; copied source remains directly indexable; Inspector exposes retained tree/source/style/state/stores, dependencies, signals, effects, timers, refs, geometry, captures, traces, and timings; retained role/name/text/semantic-ID/test-ID locators, snapshots, production-handler event dispatch, mounted actions, deterministic time commands, in-process Rust API, and opt-in correlated JSON-lines codec exist; pure-Rhai showcase/Timeline/Chat acceptance apps | GPU screenshot/readback and raw platform input commands; 120 Hz interaction certification; Rust fast-path Timeline parity app |
| J. Certification | Partial | debug and release workspace/all-target tests pass; strict all-target Clippy with dev-reload passes; `RUSTDOCFLAGS=-Dwarnings` workspace docs and release build pass; independent GPUI `test-support` suite passes 15 production-renderer keyboard/pointer/text/clipboard/overlay/embedding/automation cases with no deleted privileged node API; unattended release-smoke launches every product example and data-table state without panic; current release policy/reconciler probe is recorded | final API/node/style/event matrices and property/failure expansions; deterministic screenshot/accessibility/hot-reload resource evidence; pure-Rhai Timeline GPUI flush/layout/paint p95 and 120 Hz result; unlocked macOS theme/locale/RTL/resize/reduced-motion/IME/manual interaction gate |

## Current automated baseline

Run from the repository root:

```sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --features dev-reload -- -D warnings
cargo test --workspace --all-targets
```

The audit is complete only when every row is complete and the acceptance gates
in section 13 of `IMPLEMENTATION_PLAN.md` pass without exclusions.
