[package]
name = "gpui-rhai-native-keyboard-tests"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
gpui = { version = "=0.2.2", default-features = false, features = ["font-kit", "test-support"] }
gpui-rhai = { path = "../../crates/gpui-rhai" }
